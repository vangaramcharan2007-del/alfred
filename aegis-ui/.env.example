# Use the host laptop's LAN address when teammates open the dashboard remotely.
NEXT_PUBLIC_AEGIS_BACKEND_URL=http://127.0.0.1:8000
# Demo-only shared token. It is visible to browsers; do not reuse a real secret here.
NEXT_PUBLIC_AEGIS_TOKEN=replace-with-demo-token
